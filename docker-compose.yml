services:
  fleetpilot:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/fleetpilot.db
      - MAX_CONCURRENT_SOLVES=1
      - NODE_ENV=production
    restart: unless-stopped
