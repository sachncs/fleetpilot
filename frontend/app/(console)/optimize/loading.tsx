import { SkeletonCard } from '@/components/shared/skeleton-card';

export default function OptimizeLoading(): React.ReactElement {
  return (
    <div className="p-6">
      <SkeletonCard />
    </div>
  );
}
